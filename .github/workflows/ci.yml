name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: pip install -e ".[dev]"
      - name: Lint
        run: ruff check .
      - name: Test
        run: pytest -q
      - name: The sample data regenerates identically
        run: |
          python tools/make_data.py
          git diff --exit-code data/
      - name: Train end to end
        run: |
          gboost train data/housing.csv
          gboost train data/churn.csv --early-stopping 20
          gboost importance data/churn.csv --depth 6 --min-leaf 2 --reg-lambda 0
